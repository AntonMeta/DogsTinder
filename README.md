# Tinder dla Psów 🐶

Aplikacja łącząca backend w Pythonie (FastAPI) z frontendem we Flutterze.

## 🚀 Uruchomienie

### 1. Backend (Serwer)
Wymaga zainstalowanego Pythona.

```bash
cd backend
python3 -m uvicorn main:app --reload
```

### 2. Frontend (Aplikacja)
Wymaga zainstalowanego Fluttera.
Otwórz **nowe okno terminala** (nie zamykaj tego z Pythonem).

```bash
cd frontend
flutter run
```